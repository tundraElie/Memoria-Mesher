Note: geomview must be installed.

Intructions:

%> mkdir build
%> cd build
%> cmake ../src/
%> make
%> cd ../examples
%> ../build/viewer -v test2.mvm


Execute viewer without arguments to see all the input file options.
%> ./viewer

2021, Sept. 7
Claudio Lobos.


